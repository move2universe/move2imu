% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/acc_calibration.R
\name{acc_calibration}
\alias{acc_calibration}
\alias{as_acc_calibration}
\title{Create calibration functions for raw acceleration values}
\usage{
acc_calibration(
  manufacturer = NULL,
  tag_id = NULL,
  sensitivity = NULL,
  offset = NULL,
  offset_x = offset,
  offset_y = offset,
  offset_z = offset,
  slope = NULL,
  slope_x = slope,
  slope_y = slope,
  slope_z = slope,
  orientation = NULL,
  orientation_x = orientation,
  orientation_y = orientation,
  orientation_z = orientation,
  units = "m/s^2",
  axes = "XYZ"
)

as_acc_calibration(df)
}
\arguments{
\item{manufacturer}{Manufacturer of the tag. Currently, \code{"eobs"} and
\code{"ornitela"} are supported. For other manufacturers, leave \code{NULL} and
manually specify the calibration parameters below.}

\item{tag_id}{If \code{manufacturer = "eobs"}, the e-obs tag ID for the tag. See
details.}

\item{sensitivity}{If \code{manufacturer = "eobs"}, the sensitivity of the tag.
Defaults to \code{"low"} if none provided. See details.}

\item{offset, offset_x, offset_y, offset_z}{Custom offset to use when
calibrating. To specify axis-specific offsets, use \code{offset_x},
\code{offset_y}, and/or \code{offset_z}.

Required if no \code{manufacturer} is specified.}

\item{slope, slope_x, slope_y, slope_z}{Custom slope to use when
calibrating. To specify axis-specific slope, use \code{slope_x},
\code{slope_y}, and/or \code{slope_z}.

Required if no \code{manufacturer} is specified.}

\item{orientation, orientation_x, orientation_y, orientation_z}{Either \code{1} or
\code{-1} indicating the orientation of the tag's axes. To
specify axis-specific orientations, use \code{orientation_x}, \code{orientation_y},
and/or \code{orientation_z}. Defaults to \code{1}.

This is useful to standardize orientations across tags of different
manufacturers or generations.}

\item{units}{Output units. Either \code{"m/s^2"} (default) or \code{"standard_free_fall"}.}

\item{axes}{Character string specifying which axes to calibrate, e.g.,
\code{"XYZ"} (default), \code{"XY"}, or \code{"Z"}. Only these axes will appear in the
calibrated output.}

\item{df}{data.frame containing columns corresponding to the available
arguments in \code{acc_calibration()}}
}
\value{
An \code{acc_calibration} object.
}
\description{
Generate an \code{acc_calibration} object containing a list of functions
with various calibration parameters to be used in \code{\link[=transform_imu]{transform_imu()}}.
\itemize{
\item Use \code{acc_calibration()} to specify calibration parameters manually.
Arguments are vectorized and matched by index.
\item Use \code{as_acc_calibration()} to convert a data.frame containing row-wise
burst calibration parameters to an \code{acc_calibration} object.
}

This allows you to specify burst-specific calibration functions to
flexibly convert raw acceleration values to physical units in \code{acc} vectors
that contain data from heterogeneous sources.
}
\details{
Tags from e-obs have default calibration functions that vary depending on the
tag's generation. Use \code{\link[=eobs_default_specs]{eobs_default_specs()}} for a summary table showing the
default offset, slope, and orientation parameters used for each e-obs
tag ID. The tag ID defines the tag generation. Note that tags from generation
1 could be set either to low or high sensitivity, each with their own
default calibration parameters.
}
\examples{
# Calibration for ornitela tags:
acc_calibration(manufacturer = "ornitela")

# E-obs tag defaults vary by tag_id and sensitivity (default `"low"`)
acc_calibration(manufacturer = "eobs", tag_id = 1000, sensitivity = "high")
acc_calibration(manufacturer = "eobs", tag_id = 4000)

# Provide vector arguments to generate element-wise calibrations:
acc_calibration(
  manufacturer = c("eobs", "ornitela"),
  tag_id = c(1000, NA)
)

# Calibration with explicit offset and slope
acc_calibration(offset = 2048, slope = 1 / 512)

# Calibrate specific axes with axis-specific args:
cal <- acc_calibration(
  offset_x = 2048, 
  offset_y = 2046,
  offset_z = 2048,
  slope = 1 / 512, 
  orientation_y = -1 # Flip y axis orientation
)

# Apply calibration with transform_imu()
transform_imu(acc_example(), cal)

# Convert a data.frame of calibration specs into a calibration vector
# (Useful if specifications are stored as metadata alongside acc bursts)
cal <- as_acc_calibration(
  data.frame(manufacturer = "eobs", tag_id = c(1000, 4000))
)
}
\seealso{
\code{\link[=transform_imu]{transform_imu()}} to apply calibration functions to the entries
in an \code{acc} vector.
}
