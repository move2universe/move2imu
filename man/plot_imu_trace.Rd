% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_imu_trace.R
\name{plot_imu_trace}
\alias{plot_imu_trace}
\title{Plot IMU traces over time}
\usage{
plot_imu_trace(x, ylab = "Value")
}
\arguments{
\item{x}{An IMU vector (\code{acc}, \code{mag}, or \code{gyro})}

\item{ylab}{A character with the y axis label}
}
\description{
Plot the trace of IMU values from an IMU vector with time on the x-axis.
}
\details{
If the bursts in the input come from multiple sources, traces may be
combined incorrectly. See examples.
}
\examples{
\dontshow{if (rlang::is_installed(c("dygraphs", "move2"))) withAutoprint(\{ # examplesIf}
plot_imu_trace(acc_example())

# If bursts come from multiple sources (in this case, deployments),
# then lines from different bursts may be incorrectly connected:
alb <- albatrosses()
a <- as_acc(alb)

plot_imu_trace(a)

# To avoid this issue, plot only a single deployment's values:
plot_imu_trace(a[move2::mt_track_id(alb) == "4261-2228"])
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=plot_sampling_effort]{plot_sampling_effort()}} to plot sample collection times.
}
