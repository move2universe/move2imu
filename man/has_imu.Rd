% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/has_imu.R
\name{has_imu}
\alias{has_imu}
\alias{has_acc}
\alias{has_mag}
\alias{has_gyro}
\title{Identify rows in a \code{move2} that contain IMU data}
\usage{
has_acc(x, colset = NULL)

has_mag(x, colset = NULL)

has_gyro(x, colset = NULL)
}
\arguments{
\item{x}{A \code{move2} object.}

\item{colset}{An \code{imu_colset} object or list of \code{imu_colset} objects
specifying the columns to check for IMU data. By default, all active
colsets detected in \code{x} are considered (see \code{\link[=active_acc_colsets]{active_acc_colsets()}}).}
}
\value{
A logical vector the same length as \code{nrow(x)}. \code{TRUE} values
indicate rows where IMU data is present under at least one active colset.
}
\description{
These functions return logical vectors indicating the rows of an input
\code{move2} object that contain IMU data for the specified sensor.
}
\details{
If \code{x} has data in more than one active IMU column set, \verb{has_*()} will return
\code{TRUE}. However, these rows cannot be parsed by \code{\link[=as_acc]{as_acc()}}, \code{\link[=as_mag]{as_mag()}}, etc.
as they contain duplicated IMU data. To ensure that \verb{has_*()} only considers
certain column sets, use the \code{colset} argument.

If no active colset is detected (e.g. a \code{move2} with only GPS data),
\verb{has_*()} returns \code{FALSE} for all rows.

For long-format data (where multiple rows compose a single burst)
all rows that contain IMU data are flagged \code{TRUE}. However, the output
of \verb{as_*()} will not necessarily return bursts at each of these locations,
as multiple of these rows will be combined into a single burst.
}
\examples{
alb <- albatrosses()

head(has_acc(alb))

# Filter to rows with acc data without building bursts
nrow(alb[has_acc(alb), ])
}
\seealso{
\code{\link[=as_acc]{as_acc()}}, \code{\link[=as_mag]{as_mag()}}, \code{\link[=as_gyro]{as_gyro()}} to extract IMU data from a
\code{move2} object.

\code{\link[=active_acc_colsets]{active_acc_colsets()}}, \code{\link[=active_mag_colsets]{active_mag_colsets()}}, \code{\link[=active_gyro_colsets]{active_gyro_colsets()}}
to inspect which colsets are detected in \code{x}.
}
